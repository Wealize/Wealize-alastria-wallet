import { Text } from 'react-native'
import styled from 'styled-components'

import { Colors } from '../../utils/themes'

export const DocumentLink = styled(Text)`
  text-align: right;
  margin-left: auto;
  color: ${Colors.activeInput};
`
